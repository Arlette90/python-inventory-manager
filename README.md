# python-inventory-manager
A modular command-line interface (CLI) application designed to manage book and author records in a local SQLite database. This project focuses on CRUD (Create, Read, Update, Delete) operations while prioritizing secure database interaction
